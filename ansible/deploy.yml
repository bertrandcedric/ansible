- hosts: all
  gather_facts: yes
  remote_user: deploy
  roles:
    - { role: test }

- hosts: client
  gather_facts: yes
  remote_user: deploy
  roles:
    - { role: mongodb/commons }

- hosts: server
  gather_facts: yes
  remote_user: deploy
  roles:
    - { role: mongodb/commons }
    - { role: mongodb/server }
